// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Belt_Array = require("bs-platform/lib/js/belt_Array.js");

function defaultErrorMessage(stringify, name, value) {
  var valueString = stringify !== undefined ? stringify(value) : "" + value;
  return "Invalid value \"" + valueString + "\" passed to \"" + name + "\"";
}

function getName(validator) {
  return validator.name;
}

function updateName(validator, name) {
  switch (validator.TAG | 0) {
    case /* Validator */0 :
        return {
                TAG: /* Validator */0,
                name: name,
                validate: validator.validate,
                message: validator.message
              };
    case /* AND */1 :
        return {
                TAG: /* AND */1,
                name: name,
                left: validator.left,
                right: validator.right
              };
    case /* OR */2 :
        return {
                TAG: /* OR */2,
                name: name,
                left: validator.left,
                right: validator.right
              };
    case /* XOR */3 :
        return {
                TAG: /* XOR */3,
                name: name,
                left: validator.left,
                right: validator.right
              };
    case /* NOT */4 :
        return {
                TAG: /* NOT */4,
                name: name,
                right: validator.right
              };
    
  }
}

function notName(a) {
  var name = a.name;
  return "NOT[ " + name + " ]";
}

function andName(a, b) {
  var nameA = a.name;
  var nameB = b.name;
  return "AND[ " + nameA + ", " + nameB + " ]";
}

function orName(a, b) {
  var nameA = a.name;
  var nameB = b.name;
  return "OR[ " + nameA + ", " + nameB + " ]";
}

function xorName(a, b) {
  var nameA = a.name;
  var nameB = b.name;
  return "XOR[ " + nameA + ", " + nameB + " ]";
}

function make(name, message, validate) {
  return {
          TAG: /* Validator */0,
          name: name,
          validate: validate,
          message: message !== undefined ? message : (function (value) {
                return defaultErrorMessage(undefined, name, value);
              })
        };
}

function and_(left, right) {
  if (left.TAG !== /* AND */1) {
    return {
            TAG: /* AND */1,
            name: andName(left, right),
            left: left,
            right: [right]
          };
  }
  var l1 = left.left;
  var arr = Belt_Array.sliceToEnd(left.right, 0);
  if (right.TAG === /* AND */1) {
    var r2 = right.right;
    var r2Length = r2.length;
    arr.push(right.left);
    for(var i = 0; i < r2Length; ++i){
      arr.push(r2[i]);
    }
    return {
            TAG: /* AND */1,
            name: andName(left, right),
            left: l1,
            right: arr
          };
  }
  arr.push(right);
  return {
          TAG: /* AND */1,
          name: andName(left, right),
          left: l1,
          right: arr
        };
}

function or_(left, right) {
  if (left.TAG !== /* OR */2) {
    return {
            TAG: /* OR */2,
            name: orName(left, right),
            left: left,
            right: [right]
          };
  }
  var l1 = left.left;
  var arr = Belt_Array.sliceToEnd(left.right, 0);
  if (right.TAG === /* OR */2) {
    var r2 = right.right;
    var r2Length = r2.length;
    arr.push(right.left);
    for(var i = 0; i < r2Length; ++i){
      arr.push(r2[i]);
    }
    return {
            TAG: /* OR */2,
            name: orName(left, right),
            left: l1,
            right: arr
          };
  }
  arr.push(right);
  return {
          TAG: /* OR */2,
          name: orName(left, right),
          left: l1,
          right: arr
        };
}

function xor(left, right) {
  return {
          TAG: /* XOR */3,
          name: xorName(left, right),
          left: left,
          right: right
        };
}

function not_(right) {
  return {
          TAG: /* NOT */4,
          name: notName(right),
          right: right
        };
}

function andU(left, right) {
  if (left.TAG !== /* AND */1) {
    return {
            TAG: /* AND */1,
            name: andName(left, right),
            left: left,
            right: [right]
          };
  }
  var l1 = left.left;
  var arr = Belt_Array.sliceToEnd(left.right, 0);
  if (right.TAG === /* AND */1) {
    var r2 = right.right;
    var r2Length = r2.length;
    arr.push(right.left);
    for(var i = 0; i < r2Length; ++i){
      arr.push(r2[i]);
    }
    return {
            TAG: /* AND */1,
            name: andName(left, right),
            left: l1,
            right: arr
          };
  }
  arr.push(right);
  return {
          TAG: /* AND */1,
          name: andName(left, right),
          left: l1,
          right: arr
        };
}

function orU(left, right) {
  if (left.TAG !== /* OR */2) {
    return {
            TAG: /* OR */2,
            name: orName(left, right),
            left: left,
            right: [right]
          };
  }
  var l1 = left.left;
  var arr = Belt_Array.sliceToEnd(left.right, 0);
  if (right.TAG === /* OR */2) {
    var r2 = right.right;
    var r2Length = r2.length;
    arr.push(right.left);
    for(var i = 0; i < r2Length; ++i){
      arr.push(r2[i]);
    }
    return {
            TAG: /* OR */2,
            name: orName(left, right),
            left: l1,
            right: arr
          };
  }
  arr.push(right);
  return {
          TAG: /* OR */2,
          name: orName(left, right),
          left: l1,
          right: arr
        };
}

function xorU(left, right) {
  return {
          TAG: /* XOR */3,
          name: xorName(left, right),
          left: left,
          right: right
        };
}

function notU(right) {
  return {
          TAG: /* NOT */4,
          name: notName(right),
          right: right
        };
}

var Infix = {
  $amp$amp: and_,
  $pipe$pipe: or_,
  $caret$caret: xor,
  not: not_
};

function _makeError(stringify, validator, value) {
  if (validator.TAG === /* Validator */0) {
    return {
            value: value,
            validator: validator,
            name: validator.name,
            message: validator.message(value)
          };
  }
  var name = validator.name;
  return {
          value: value,
          validator: validator,
          name: name,
          message: defaultErrorMessage(stringify, name, value)
        };
}

function _evalSync(resultsDict, errStackRef, value, validator) {
  var errStack = errStackRef.contents;
  switch (validator.TAG | 0) {
    case /* Validator */0 :
        var name = validator.name;
        if (validator.validate(value)) {
          resultsDict[name] = true;
          return true;
        } else {
          resultsDict[name] = false;
          errStack[errStack.length] = validator;
          return false;
        }
    case /* AND */1 :
        var name$1 = validator.name;
        if (_evalSync(resultsDict, errStackRef, value, validator.left)) {
          if (_ANDArrayEval(resultsDict, errStackRef, value, validator.right)) {
            if (errStack.length !== 0) {
              errStackRef.contents = [];
            }
            resultsDict[name$1] = true;
            return true;
          } else {
            errStack[errStack.length] = validator;
            resultsDict[name$1] = false;
            return false;
          }
        } else {
          errStack[errStack.length] = validator;
          resultsDict[name$1] = false;
          return false;
        }
    case /* OR */2 :
        var name$2 = validator.name;
        if (_evalSync(resultsDict, errStackRef, value, validator.left)) {
          if (errStack.length !== 0) {
            errStackRef.contents = [];
          }
          resultsDict[name$2] = true;
          return true;
        } else if (_ORArrayEval(resultsDict, errStackRef, value, validator.right)) {
          if (errStack.length !== 0) {
            errStackRef.contents = [];
          }
          resultsDict[name$2] = true;
          return true;
        } else {
          errStack[errStack.length] = validator;
          resultsDict[name$2] = false;
          return false;
        }
    case /* XOR */3 :
        var name$3 = validator.name;
        var match = _evalSync(resultsDict, errStackRef, value, validator.left);
        var match$1 = _evalSync(resultsDict, errStackRef, value, validator.right);
        if (match) {
          if (match$1) {
            errStack[errStack.length] = validator;
            resultsDict[name$3] = false;
            return false;
          } else {
            if (errStack.length !== 0) {
              errStackRef.contents = [];
            }
            resultsDict[name$3] = true;
            return true;
          }
        } else if (match$1) {
          if (errStack.length !== 0) {
            errStackRef.contents = [];
          }
          resultsDict[name$3] = true;
          return true;
        } else {
          errStack[errStack.length] = validator;
          resultsDict[name$3] = false;
          return false;
        }
    case /* NOT */4 :
        var right = validator.right;
        var name$4 = validator.name;
        switch (right.TAG | 0) {
          case /* Validator */0 :
              if (right.validate(value)) {
                errStack[errStack.length] = validator;
                resultsDict[name$4] = false;
                return false;
              } else {
                if (errStack.length !== 0) {
                  errStackRef.contents = [];
                }
                resultsDict[name$4] = true;
                return true;
              }
          case /* NOT */4 :
              var res = _evalSync(resultsDict, errStackRef, value, right.right);
              resultsDict[name$4] = res;
              return res;
          default:
            if (_evalSync(resultsDict, errStackRef, value, right)) {
              errStack[errStack.length] = validator;
              resultsDict[name$4] = false;
              return false;
            } else {
              if (errStack.length !== 0) {
                errStackRef.contents = [];
              }
              resultsDict[name$4] = true;
              return true;
            }
        }
    
  }
}

function _ANDArrayEval(resultsDict, errStackRef, value, validatorArray) {
  var len = validatorArray.length;
  var result = true;
  for(var i = 0; i < len; ++i){
    var current = _evalSync(resultsDict, errStackRef, value, validatorArray[i]);
    if (current === false) {
      result = false;
    }
    
  }
  return result;
}

function _ORArrayEval(resultsDict, errStackRef, value, validatorArray) {
  var len = validatorArray.length;
  var result = false;
  for(var i = 0; i < len; ++i){
    var current = _evalSync(resultsDict, errStackRef, value, validatorArray[i]);
    if (current === true) {
      result = true;
    }
    
  }
  return result;
}

function validate(stringify, validator, value) {
  var name = validator.name;
  var errStackRef = {
    contents: []
  };
  var boolMap = {};
  var result = _evalSync(boolMap, errStackRef, value, validator);
  if (result) {
    return {
            TAG: /* Ok */0,
            _0: {
              value: value,
              name: name,
              validator: validator,
              errors: [],
              errorMap: {},
              resultTree: undefined,
              boolMap: boolMap
            }
          };
  }
  var errorsArray = [];
  var errorsDict = {};
  var errStack = errStackRef.contents;
  var errorsIndexRef = 0;
  for(var i = 0 ,i_finish = errStack.length; i < i_finish; ++i){
    var errorsIndex = errorsIndexRef;
    var v = Belt_Array.get(errStack, i);
    if (v !== undefined) {
      var currentError = _makeError(stringify, v, value);
      errorsArray[errorsIndex] = currentError;
      errorsDict[v.name] = currentError;
      errorsIndexRef = errorsIndexRef + 1 | 0;
    }
    
  }
  return {
          TAG: /* Error */1,
          _0: {
            value: value,
            name: name,
            validator: validator,
            errors: errorsArray,
            errorMap: errorsDict,
            resultTree: undefined,
            boolMap: boolMap
          }
        };
}

function getName$1(validator) {
  return validator.name;
}

function rename(validator, name) {
  switch (validator.TAG | 0) {
    case /* Validator_Async */0 :
        return {
                TAG: /* Validator_Async */0,
                name: name,
                validate: validator.validate,
                message: validator.message
              };
    case /* AND_Async */1 :
        return {
                TAG: /* AND_Async */1,
                name: name,
                a: validator.a,
                b: validator.b
              };
    case /* OR_Async */2 :
        return {
                TAG: /* OR_Async */2,
                name: name,
                a: validator.a,
                b: validator.b
              };
    case /* XOR_Async */3 :
        return {
                TAG: /* XOR_Async */3,
                name: name,
                a: validator.a,
                b: validator.b
              };
    case /* NOT_Async */4 :
        return {
                TAG: /* NOT_Async */4,
                name: name,
                a: validator.a
              };
    
  }
}

var Async = {
  getName: getName$1,
  rename: rename
};

var Impl = {
  defaultErrorMessage: defaultErrorMessage,
  getName: getName,
  updateName: updateName,
  notName: notName,
  andName: andName,
  orName: orName,
  xorName: xorName,
  make: make,
  and_: and_,
  or_: or_,
  xor: xor,
  not_: not_,
  andU: andU,
  orU: orU,
  xorU: xorU,
  notU: notU,
  Infix: Infix,
  _makeError: _makeError,
  _evalSync: _evalSync,
  _ANDArrayEval: _ANDArrayEval,
  _ORArrayEval: _ORArrayEval,
  validate: validate,
  Async: Async
};

exports.Impl = Impl;
exports.defaultErrorMessage = defaultErrorMessage;
exports.getName = getName;
exports.updateName = updateName;
exports.notName = notName;
exports.andName = andName;
exports.orName = orName;
exports.xorName = xorName;
exports.make = make;
exports.and_ = and_;
exports.or_ = or_;
exports.xor = xor;
exports.not_ = not_;
exports.andU = andU;
exports.orU = orU;
exports.xorU = xorU;
exports.notU = notU;
exports.Infix = Infix;
exports._makeError = _makeError;
exports._evalSync = _evalSync;
exports._ANDArrayEval = _ANDArrayEval;
exports._ORArrayEval = _ORArrayEval;
exports.validate = validate;
exports.Async = Async;
/* No side effect */
